const addLesson = (element, type) => {
    return {
        type,
        element
    }
}

const configureLesson = (element, type) => {
    return {
        type,
        element
    }
}

export { addLesson, configureLesson }
