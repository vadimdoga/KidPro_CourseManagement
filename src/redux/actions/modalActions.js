export const modifyModalState = (element, type) => {
    return {
        type,
        element
    }
}

export const modifyModalID = (element, type) => {
    return {
        type,
        element
    }
}

export const modifyModalData = (element, type) => {
    return {
        type,
        element
    }
}

export const modifyModalTag = (element, type) => {
    return {
        type,
        element
    }
}
